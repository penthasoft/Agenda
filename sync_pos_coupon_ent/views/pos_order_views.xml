<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_pos_pos_form_coupons" model="ir.ui.view">
        <field name="name">pos.order.form.coupons</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_pos_form"/>
        <field name="arch" type="xml">
            <field name="lines" position="after">
                <field name="amount_applied_coupons_total" invisible="1"/>
                <field name="amount_applied_promo_total" invisible="1"/>
            </field>
        </field>
    </record>

</odoo>